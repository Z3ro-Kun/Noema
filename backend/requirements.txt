fastapi==0.118.0
uvicorn[standard]==0.38.0
pydantic==2.11.7
pydantic-settings==2.7.0
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.15.1
pgvector==0.3.6
redis==5.2.1
rq==2.1.0
httpx==0.28.1

# Embeddings. Noema is CPU-first: install the CPU-only torch wheel FIRST so
# pip does not pull ~2.5GB of CUDA libraries that nothing here uses --
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# then install this file. A GPU is never required.
torch==2.5.1
sentence-transformers==3.3.1

pytest==8.3.4
pytest-asyncio==0.25.0
