fastapi==0.118.0
uvicorn[standard]==0.38.0
pydantic==2.11.7
pydantic-settings==2.7.0
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.15.1
pgvector==0.3.6
redis==5.2.1
rq==2.1.0
httpx==0.28.1

# Embeddings. Both processes need these: the worker encodes the corpus, and
# the API encodes a *query* for theme search (`POST /api/v1/search/works`),
# which loads the same model into the API process on first use.
#
# Noema is CPU-first. Install the CPU-only torch wheel FIRST so pip does not
# pull ~2.5 GB of CUDA libraries that nothing here uses --
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# then install this file. The Dockerfiles do exactly that. A GPU is never
# required.
torch==2.5.1
sentence-transformers==3.3.1
