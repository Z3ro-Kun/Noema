# The API origin this build talks to.
#
# Required for a production build: `vite.config.ts` refuses to build without
# it, and on a build machine (CI=true) refuses a localhost or non-https value.
VITE_API_URL=http://localhost:8000
